services:

  backend:
    build: ./backend/
    ports:
      - 8080:8080
    restart: always

  frontend:
    build: ./frontend/
    ports:
      - 3000:3000
    environment:
    - NODE_OPTIONS=--openssl-legacy-provider
    restart: always
    
